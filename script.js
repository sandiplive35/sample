const qualityStyle=document.createElement("style");qualityStyle.textContent=`.thumb-canvas{aspect-ratio:16/9;background:#090a0f}.thumb-canvas img{display:block;width:100%;height:auto;aspect-ratio:16/9;object-fit:contain;transform:none!important;filter:none!important;image-rendering:auto;-webkit-image-rendering:optimize-contrast;backface-visibility:hidden}.thumb-card:hover .thumb-canvas img{transform:none!important}.thumb-card{will-change:auto}`;document.head.appendChild(qualityStyle);document.querySelectorAll("[data-thumb]").forEach(image=>{const key=image.dataset.thumb;const source=window.THUMB_DATA&&window.THUMB_DATA[key];if(source){image.src=source;image.decoding="sync";image.loading="eager"}else{image.alt="Thumbnail preview unavailable"}});const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}})},{threshold:.12,rootMargin:"0px 0px -40px 0px"});document.querySelectorAll(".reveal").forEach(element=>observer.observe(element));document.getElementById("year").textContent=new Date().getFullYear();const railLinks=[...document.querySelectorAll(".rail-nav a")];const sections=railLinks.map(link=>document.querySelector(link.getAttribute("href"))).filter(Boolean);const setActiveRailLink=()=>{const current=sections.reduce((closest,section)=>{const distance=Math.abs(section.getBoundingClientRect().top-160);return!closest||distance<closest.distance?{section,distance}:closest},null);railLinks.forEach(link=>{const active=current&&link.getAttribute("href")===`#${current.section.id}`;link.style.background=active?"#12131c":"";link.style.color=active?"#fff":""})};window.addEventListener("scroll",setActiveRailLink,{passive:true});setActiveRailLink();